# Copy to .env (gitignored). Never commit real passwords.
POSTGRES_DB=sonar
POSTGRES_USER=sonar
POSTGRES_PASSWORD=change-me-strong
SONAR_HTTP_PORT=9000
# Local Docker Desktop: set true if bootstrap checks fail on vm.max_map_count
# SONAR_ES_BOOTSTRAP_CHECKS_DISABLE=true
